Placeholder — bloc tests live in:
  test/blocs/ (existing)
  test/widgets/ (for widget tests that use blocs)

This directory is reserved for pure unit tests of blocs (no widget tree).
Remove this file once bloc unit tests are created.
